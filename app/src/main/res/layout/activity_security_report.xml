<?xml version="1.0" encoding="utf-8"?>
<!-- ScrollView lets learners read the full generated security report. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <!-- ConstraintLayout holds the report title, content, and Retour button. -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="20dp">

        <!-- Title identifies the screen that scans activities, services, and receivers. -->
        <TextView
            android:id="@+id/textViewReportTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Security Report"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- TextView displays the PackageManager component scan and recommendations. -->
        <TextView
            android:id="@+id/textViewSecurityReport"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="Report loading"
            android:textSize="15sp"
            android:typeface="monospace"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textViewReportTitle" />

        <!-- Retour button calls finish() and keeps navigation behavior easy to observe. -->
        <Button
            android:id="@+id/buttonReportBack"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="20dp"
            android:text="Retour"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textViewSecurityReport" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
